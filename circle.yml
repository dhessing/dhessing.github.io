machine:
  environment:
    USER_NAME: dhessing
    USER_EMAIL: dhessing@gmail.com
  ruby:
    version: 2.2.3

dependencies:
  pre:
    - ./bin/setup

test:
  post:
    - bundle exec jekyll build
    - bundle exec htmlproof ./_site --only-4xx --href-ignore "#"

deployment:
  production:
    branch: source
    commands:
      - ./bin/automated